# ============================================================
# DeforestNet - Requirements for Render.com Deployment
# CPU-only PyTorch to keep deployment size manageable
# ============================================================

# Core ML/DL (CPU-only - MUCH smaller than GPU version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# Data Processing
numpy>=1.24.0
scipy>=1.10.0

# Image Processing
pillow>=10.0.0
scikit-image>=0.21.0
opencv-python-headless>=4.8.0

# Machine Learning Utilities
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0

# Web Framework (API + Dashboard)
flask>=3.0.0
flask-cors>=4.0.0

# HTML Parsing (for verification)
beautifulsoup4>=4.12.0

# HTTP Requests (for Telegram API)
requests>=2.31.0

# Configuration
python-dotenv>=1.0.0

# Logging
colorlog>=6.7.0

# Progress Bars
tqdm>=4.65.0

# Production WSGI Server (use --workers 1 on 512MB free tier)
gunicorn>=21.2.0
